# قواعد أمان Firebase — بيمبو جرد

## طريقة التفعيل
1. افتح [Firebase Console](https://console.firebase.google.com) → اختار المشروع (jard-86baf)
2. من القائمة الجانبية: **Realtime Database** → تبويب **Rules**
3. امسح المحتوى الموجود، والصق محتوى ملف `firebase-rules.json` كامل
4. اضغط **Publish**

## اللي القواعد دي بتعمله فعلياً
- بتقفل قاعدة البيانات كلها **ما عدا** مسار `jard/` بس — لو حد حاول يقرا أو يكتب في أي مسار تاني في نفس المشروع، هيترفض.
- بتتأكد إن أي كتابة جديدة على `jard/data` فيها `rev` رقمي على الأقل (حماية بسيطة من كتابة بيانات تالفة الشكل بالغلط).
- بتتأكد إن أي إشعار أو جلسة مكتوبة فيها `ts` (وقت) رقمي، بنفس المنطق.

## اللي القواعد دي **مش** بتعمله (وليه)
البرنامج مبنى بدون **Firebase Authentication** — يعني مفيش تسجيل دخول حقيقي معترف بيه من Firebase نفسه، كل نظام "الأدمن/المستخدم" حالياً بيتحقق منه في كود المتصفح بس (JavaScript). ده معناه:

- **مينفعش نمنع الكتابة/القراءة حسب الدور (أدمن مقابل مستخدم عادي) من جوه القواعد** — لأن Firebase مالوش أي فكرة مين اللي بيبعت الطلب أصلاً. أي حد معاه رابط قاعدة البيانات (اللي هو أصلاً مكتوب في مصدر الصفحة العامة) يقدر يقرا/يكتب في مسار `jard/` كله، حتى لو مش فاتح البرنامج خالص.
- القواعد دي بتحمي بس من: (1) وصول حد لأجزاء تانية من نفس مشروع Firebase لو فيه، و(2) كتابة بيانات تالفة الشكل بالغلط.

## الحل الحقيقي الكامل (شغل مستقبلي منفصل)
لو عايز حماية حقيقية (يعني مستخدم عادي فعلياً مايقدرش يمسح البيانات حتى لو فتح Developer Tools وبعت طلب مباشر)، الطريق الوحيد هو دمج **Firebase Authentication** (تسجيل دخول حقيقي بإيميل/باسورد أو حتى مجهول مع custom claims للدور)، وبعدين كتابة قواعد زي:

```json
"jard": {
  "data": {
    ".read": "auth != null",
    ".write": "auth != null && root.child('jard/meta/users/'+auth.uid+'/role').val() == 'admin'"
  }
}
```

ده تغيير معماري أكبر بيمس تسجيل الدخول في البرنامج كله (مش مجرد قواعد)، وعشان كده مش جزء من هذا التعديل — لو عايز نبدأ فيه، قولّي في محادثة منفصلة.

## بديل أسرع وأخف: Firebase App Check
لو مش عايز تدخل في Firebase Authentication دلوقتي، أقل مجهود ممكن يقلل الاستغلال العشوائي (بوتات بتدور على قواعد بيانات مفتوحة) هو تفعيل **App Check** من Firebase Console، وده بيتأكد إن الطلبات جايه فعلاً من صفحة البرنامج نفسها مش من أي حد بيبعت طلبات REST يدوي. ده مش بديل عن الحماية الحقيقية لكنه خط دفاع إضافي بسيط.
